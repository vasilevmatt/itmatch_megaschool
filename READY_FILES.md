# 📝 Готовые файлы

Все необходимые артефакты для фронтенд-демо собраны:

## Конфигурация
- `docker-compose.timeweb.yml` — один сервис `frontend` (nginx + статические файлы).
- `docker-compose.dev.yml` — дев-сервис `frontend` с Vite.
- `env.example` — пример (опционально `VITE_APP_TITLE`).
- `.gitignore` — защищает чувствительные данные.

## Скрипты
- `deploy.sh` / `deploy.bat` — простой запуск Compose.

## Документация
- `README.md` — описание фронтенд-демо.
- `DEPLOY_GUIDE.md` — короткий гид по деплою.

## Код
- `tg-web-app/` — весь рабочий фронтенд, включая мок-API.

Backend удалён из пайплайна; все данные генерируются в браузере.
