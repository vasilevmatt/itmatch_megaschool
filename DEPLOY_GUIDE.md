# 🚀 Деплой фронтенда на TimeWeb Cloud

Проект теперь состоит только из статического фронтенда. Никакой базы, бэкенда или дополнительных сервисов не требуется.

## Что понадобится
- Аккаунт TimeWeb Cloud (или любой Docker-хост).
- Репозиторий с этим кодом.

## Шаги
1. **Загрузите код в GitHub.**
2. **Создайте приложение** в TimeWeb Cloud:
   - Тип: Docker → Docker Compose.
   - Файл: `docker-compose.timeweb.yml`.
3. **Запустите сборку.**  
   Файл `docker-compose.timeweb.yml` поднимет единственный сервис `frontend` (nginx + собранный Vite).
4. **Проверьте выдачу:**  
   После развёртывания приложение будет доступно по порту `8080` (проброшен на 5173 внутри контейнера).

## Переменные окружения
Не обязательны. При желании можете задать `VITE_APP_TITLE` для отображения своего заголовка.

## Проверка
```bash
docker-compose -f docker-compose.timeweb.yml ps
docker-compose -f docker-compose.timeweb.yml logs -f
```

Готово! Открывайте домен из TimeWeb Cloud — приложение работает полностью на мок-данных в браузере.
