# Tinder-like Telegram Mini App (Frontend Only)

Интерактивный прототип Tinder-подобной мини-аппы для Telegram. Теперь это **полностью фронтенд**: мок-данные живут в `localStorage`, никакой базы, бэкенда или внешних сервисов не требуется.

## Что внутри
- 🎨 Готовые страницы: анкета, свайпы, матчи, чат, профиль.
- 🪄 Мок-API (`src/services/mockApi.ts`) имитирует все запросы и хранит состояние локально.
- 📱 Поддержка Telegram WebApp SDK с фоллбэком на тестового юзера.
- 🖼️ Локальная загрузка фото (base64), плейсхолдер-аватар.

## Быстрый старт (локально)
```bash
cd tg-web-app
npm install
npm run dev
```
Откройте http://localhost:5173 — бэкенд не нужен.

## Запуск через Docker
```bash
docker-compose -f docker-compose.dev.yml up --build
```

## Переменные окружения
Файл `.env` теперь необязателен. Для примера используется только:
```
VITE_APP_TITLE="Tinder Mini App (frontend only)"
```

## Структура
```
tinder-miniapp-telegram/
├── tg-web-app/               # Vite + React фронтенд
│   ├── src/services/mockApi.ts    # Вся "логика сервера" на моках
│   ├── src/pages/                # Страницы (Swipe, Matches, Chat, Profile, Setup)
│   ├── src/components/           # UI-компоненты + SwipeCard
│   ├── src/assets/               # Плейсхолдеры
│   └── Dockerfile                # Прод-образ (nginx)
├── docker-compose.dev.yml   # Только фронтенд
├── docker-compose.timeweb.yml # Продовый образ фронтенда
├── deploy.sh / deploy.bat   # Деплой одного сервиса
└── env.example              # Минимальные env-переменные
```

## Примечания
- Данные (профиль, свайпы, матчи, чат) хранятся в `localStorage` и сбрасываются при очистке.
- Функции API имитируют задержку запросов для реалистичного UX.
- Для лайка можно нажать ❤️ или сделать двойной тап по карточке.
